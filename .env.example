VITE_API_URL=http://localhost:5000/api
VITE_SOCKET_URL=http://localhost:5000
VITE_APP_NAME=MediCare HMS

# ── WebRTC / Metered TURN (optional — improves calls on restrictive networks) ──
# Get free API key at: https://www.metered.ca/tools/openrelay
# Leave blank to use the free OpenRelay fallback (works for most networks)
VITE_METERED_API_KEY=
VITE_METERED_APP_NAME=
